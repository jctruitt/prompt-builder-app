# AI Prompt Builder

An interactive web application for building well-structured XML prompts for AI models like Claude, ChatGPT, and others. Features user accounts with encrypted API key storage, per-user prompt management, and direct Claude API/CLI integration.

## Quick Start

```bash
cd prompt-builder-app
npm install

# Terminal 1 — Start the backend server
npm run server

# Terminal 2 — Start the frontend dev server
npm run dev
```

Open http://localhost:5173, register an account, and start building prompts.

See [prompt-builder-app/README.md](prompt-builder-app/README.md) for full documentation including:
- Step-by-step local installation with Visual Studio Code
- How to run all three servers (backend, frontend, MCP)
- Claude Code CLI and MCP integration setup
- Environment variable configuration
- Troubleshooting guide
